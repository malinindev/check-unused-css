import styles from '@/AliasDirReferenceNoBaseUrl.module.css';

export const AliasDirReferenceNoBaseUrl: React.FC = () => (
  <div className={`${styles.usedClass} ${styles.usedClass2}`}>
    <div className={styles.usedClass3} />
  </div>
);
